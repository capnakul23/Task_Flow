spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:taskflow}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=${DDL_MODE:update}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
jwt.secret=${JWT_SECRET:LocalDevSecretKey@TaskFlow#2024$XyZAbCdEfGhIjKlMnOpQrStUvWx}
jwt.expiration=86400000
server.port=${PORT:8080}
management.endpoints.web.exposure.include=health
spring.jackson.time-zone=UTC
spring.jackson.serialization.write-dates-as-timestamps=false
